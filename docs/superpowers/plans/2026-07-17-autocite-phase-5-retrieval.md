# AutoCite Phase 5 Local Retrieval Plan

1. Add failing tests for licensed ingestion, stable chunk IDs, metadata-first retrieval, triggers, and score safety.
2. Add approved original Markdown summaries and an explicit inclusion manifest.
3. Implement reproducible local indexes with lexical and optional embedding backends.
4. Add an optional reranker interface and disabled experimental GLiNER adapter.
5. Feed retrieved chunk IDs into bounded Qwen prompts without weakening validation.
6. Expose source-attributed local context in review and MCP output.
7. Verify, document, publish, merge, and continue to Phase 6.
