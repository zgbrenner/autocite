# AutoCite automation

Validation and publishing are intentionally separate. Pull requests run compatibility, quality, desktop, and dependency checks. Native multi-platform packaging and publication run only after changes reach `main` or through an explicit manual dispatch.
